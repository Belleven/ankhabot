1.1.0
    -Soporte para doujinshis de nhentai
    -listas editables ahora soportan varios tipos de media
    -ahora el módulo de telegram puede usar el DankieLogger
    -setglobal y deltrigger ahora mandan los mensajes para aceptar/rechazar de forma más bonita
    -last.fm rediseñado
    -función obtener_enlace_usuario rediseñada, cachea nombres
    -/nisman ahora manda listas
    -/apodos ahora manda listas
    -solo se envía un mismo trigger por grupo cada 30 segundos como mínimo
    -ajustados valores del antiflood
    -antiflood en la pole
    -/grita -perocontodo corregido
    -corregidos errores menores
    -corregido bugazo en /ban
    -agregados varios manejos de excepciones de telegram
    -/chats iniciado + otros comandos similares
     
1.0.8
    -corregido bug en ruleta

1.0.7
    -setglobal responde al mensaje que llamo al comando al ser aceptado o rechazado
    -arreglado bug al apretar botón para borrar un trigger global
    -función para listar elementos que serán mostrados en un arreglo terminada
    -arreglado bug en /img que no permitía búsquedas con caracteres que no fueran ascii
    -Ruleta nisman rediseñada

1.0.6
    -/triggers ahora hace páginas correctamente

1.0.5
    -metadatos ahora toma correctamente el argumento "-entidades" por un dev
    -recarga ahora responde "1 bala cargada" en vez de "1 balas cargadas"
    -comando triggers los devuelve en orden lexicográfico
    -callback de set/del trigger valida que no sea presionada muchas veces

1.0.4
    -enlace_usuario_id toma cuentas eliminadas

1.0.3
    -setglobal funciona correctamente

1.0.2
    -removido raise de prueba
    -hotfix mal hecho para que ande el !img

1.0.1
    -cambiados valores de prueba

1.0.0
    -reestructuración del código
        -usa handlers y un dispatcher
        -parseo de comandos rediseñado
        -logging a un canal de telegram
        -se pueden armar botoneras de páginas de texto
        -lista de flood para comandos flooderos
    -pole
    -triggers
    -blacklist de usuarios
    -comando /trinsfirmir
    -comando /rekt
    -comando /help
    -comando /img
    -comando /sub
    -comando /escuchando y otras cosas de last.fm
    -comandos /callefuegos y /deusvult
    -comando /cp
    -comandos /di y /grita
    -comandos /apodos y /apodar
    -varios comandos de moderación
    -comando /ultimavista
    -función x2
    -ruleta nisman
    -saluda a miembros nuevos
    -corrección de errores
    -mensajes de cualquier longitud
    -estilo de código agregado

0.2.0
    -reestructurado el bot
    -método para registrar un comando 
    -comando /ping devuelve los ms que tardó
    -comando /help lista los comandos registrados
